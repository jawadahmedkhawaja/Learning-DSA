# Recursion
---

Calling of function to itself is called recursion.

## What is neccessary in recursion?

In recursion a **base case** is must to terminate the recursion else it will go for infinite recursion and will cause ***stack overflow***.

## Parametrized vs Fuctional Recursion

In function recusion we return something